data/
db/
*.log
.DS_Store
# NO subir la carpeta de datos vivos de Docker (si la tienes)
mongodb_data/
db_data/

# SÍ queremos subir el dump, así que NO debe estar aquí
# (Si ves "dump/" en este archivo, bórralo)